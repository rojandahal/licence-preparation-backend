export enum AdminRole {
  SUPER_ADMIN = 'SUPER_ADMIN', // Full system access
  MANAGER = 'MANAGER', // Advanced reporting and some system controls
  VIEWER = 'VIEWER', // Read-only access to reports
}
